## Quickstart (local)

1. Copy `.env.example` -> `.env` and fill credentials (or use `deploy/.env` for complete template).
2. pip install -r requirements.txt
3. Run locally:
   ```bash
   python Live_market_New.py
